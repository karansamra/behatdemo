default:
    autoload:
        - %paths.base%/features/bootstrap
    suites:
        default:
            contexts:
                - FeatureContext
                - SecondContext
    extensions:
        Behat\MinkExtension:
            base_url: http://uat.herbdigest.com
            goutte: ~
            selenium2: ~